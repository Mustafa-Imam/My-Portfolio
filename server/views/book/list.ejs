<%- include('../partials/header.ejs') %>

<main class="container">
    <div class="row">
        <div class="offset-md-1 col-md-10">
            <h1><%= title %></h1>
            <a href="/booklist/add" class="btn btn-primary" role="button">
                <i class="fas fa-plus-circle"></i> Add Book
            </a>
            <br>
            <table class="table table-bordered table-striped table-hover">
                <tr class="d-flex">
                    <th class="text-center col-2">Name</th>
                    <th class="text-center col-2">Author</th>
                    <th class="text-center col-1">Published</th>
                    <th class="text-center col-2">Price</th>
                    <th class="text-center col-3">Description</th>
                    <th class="text-center col-1">Action</th>
                </tr>
                <% for(let count = 0; count < booksList.length; count++) { %>
                    <tr class="d-flex">
                        <td class="text-center col-2"><%= booksList[count].Name %></td>
                        <td class="text-center col-2"><%= booksList[count].Author %></td>
                        <td class="text-center col-1"><%= booksList[count].Published %></td>
                        <td class="text-center col-2"><%= booksList[count].Price %></td>
                        <td class="text-center col-3"><%= booksList[count].Description %></td>
                        <td class="text-center col-1">
                            <a href="/booklist/edit/<%= booksList[count]._id %>" class="btn btn-primary btn-sm">
                                <i class="fas fa-pencil-alt"></i> Edit
                            </a> 
                        </td>
                        <td class="text-center col-1">
                            <a href="/booklist/delete/<%= booksList[count]._id %>" class="btn btn-danger btn-sm">
                                <i class="fas fa-trash-alt"></i> Delete
                            </a> 
                        </td>
                    </tr>
                <% } %>
            </table>
        </div>
    </div>
</main>

<%- include('../partials/footer') %>
